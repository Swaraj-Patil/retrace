# Retrace API

FastAPI ingestion service for Retrace.

## Run locally

```bash
uv run uvicorn api.main:app --reload --host 0.0.0.0 --port 8000
```

**Day 1 status:** package skeleton only. Real endpoints land Day 3.
