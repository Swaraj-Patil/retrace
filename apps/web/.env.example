# API base for the FastAPI backend. The Next server reads this; never leaked to the browser.
RETRACE_API_URL=http://localhost:8000

# Demo project API key. Server-side only. Never prefix with NEXT_PUBLIC_.
# Get a fresh one from `make seed`.
RETRACE_API_KEY=rt_replace_me
